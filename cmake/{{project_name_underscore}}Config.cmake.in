@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# If you have any dependencies, find them here
# Example:
# find_dependency(Threads REQUIRED)
# find_dependency(fmt 8.0 REQUIRED)

# Include the exported targets
include("${CMAKE_CURRENT_LIST_DIR}/{{ project_name_underscore }}Targets.cmake")

# Provide the library target for backwards compatibility
# This allows users to use both {{ project_name_underscore }}::{{ project_name_underscore }} (new) and {{ project_name_underscore }} (old) syntax
if(NOT TARGET {{ project_name_underscore }} AND TARGET {{ project_name_underscore }}::{{ project_name_underscore }})
    add_library({{ project_name_underscore }} ALIAS {{ project_name_underscore }}::{{ project_name_underscore }})
endif()

# Check that required components are available
check_required_components({{ project_name_underscore }})
